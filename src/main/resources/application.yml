spring:
  datasource:
    username: root
    password: anu@hSenid99
    url: jdbc:mysql://localhost:3306/university
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect